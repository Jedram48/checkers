package Model;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;
class BoardTest {

    @Test
    void simpleBoardTests()
    {
        /*
        Board board = new Board(5, 4, true);

        //Checking board size
        assertEquals(4, board.sizeY);
        assertEquals(5, board.sizeX);

        //Checking colors of fields
        assertEquals(Piece_color.WHITE, board.Fields[0][0].color);
        assertEquals(Piece_color.BLACK, board.Fields[4][3].color);

        //Checking if method validIndex works
        assertEquals(true, board.validIndex(4, 3));
        assertEquals(false, board.validIndex(4, 4));
        assertEquals(true, board.validIndex(0, 0));
        assertEquals(false, board.validIndex(-1, 0));
        assertEquals(false, board.validIndex(0, -1));


        //Same for another board
        Board board2 = new Board(8, 8, false);
        assertEquals(8, board2.sizeY);
        assertEquals(8, board2.sizeX);
        assertEquals(Piece_color.WHITE, board2.Fields[0][1].color);
        assertEquals(Piece_color.BLACK, board2.Fields[7][7].color);
        assertEquals(true, board2.validIndex(0, 0));
        assertEquals(true, board2.validIndex(7, 7));
        assertEquals(false, board2.validIndex(9, 0));
        assertEquals(false, board2.validIndex(-1, 0));

         */
    }

}